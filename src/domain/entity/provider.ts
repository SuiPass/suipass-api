export type ProviderEntity = {
  id: string;
  name: string;
  desc: string;
  logoUrl: string;
  submitFee: string;
  updateFee: string;
  balance: number;
  maxLevel: number;
  maxScore: number;
  disabled: boolean;
};
